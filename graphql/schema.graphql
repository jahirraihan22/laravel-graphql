# this is query
type Query {
    users: [User!]! @all
    users_paginate: [User!]! @paginate
    user(id: ID @eq): User @find # user(id:1) getting a user by query
}

# this is table
type User {
    id: ID!
    name: String!
    created_at: DateTime
    updated_at: DateTime
}
